---
# File:

- ec2_vpc_subnet_facts:
    region: "{{ aws_region }}"
  register: subnet_facts

- set_fact:
    subnet_ids: "{{ subnet_facts.subnets|map(attribute='vpc_id')|list }}"

- set_fact:
    random_vpc_id: "{{ subnet_ids | random }}"
